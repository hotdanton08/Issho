# Project: Issho

## 產品目標

建立一個以手機優先（mobile-first）為設計核心的日文助詞練習 PWA。

整體體驗可以參考 Duolingo 的簡潔與視覺互動風格，但不要複製其品牌素材、角色、商標或精確畫面。

產品內容依照《大家的日本語》的課程進度設計，但所有練習句子都必須自行原創。

最重要的產品原則是：

開啟 App → 點擊開始 → 立刻進入練習。

除非使用者明確要求，否則不要加入不必要的：

- 複雜設定
- Dashboard
- 導航分頁
- 帳號系統
- 遊戲化系統
- 後端基礎設施

---

## 技術架構

使用：

- Vue 3
- TypeScript
- Quasar CLI with Vite
- Pinia
- PWA mode

優先使用：

- Composition API
- `<script setup lang="ts">`

V0.1 不需要後端。

輕量設定使用 `localStorage` 保存。

日文句子播放使用 Web Speech API。

---

## 產品規格

實作產品功能之前，先閱讀：

`docs/PRODUCT_SPEC.md`

將這份文件視為主要產品規格。

如果實作內容與 `PRODUCT_SPEC.md` 衝突，除非使用者明確修改需求，否則以 `PRODUCT_SPEC.md` 為準。

---

## Mobile First

優先針對手機直向畫面設計。

主要參考尺寸：

`390x844`

另外也要確認：

- `375x667`
- `393x852`
- `430x932`

桌面版只需要保持可正常使用。

不要以桌面版作為主要 UX 最佳化目標。

---

## UX 原則

UI 必須保持高度視覺化、簡單、直覺。

優先使用：

- 大型可點擊按鈕
- Icon
- 圓角卡片
- 短文字標籤
- 明確的視覺回饋
- 每個畫面只突出一個主要操作

避免：

- 傳統大量表單式 UI
- 表格
- Dashboard 版型
- 不必要的下拉選單
- 過多說明文字

首頁必須永遠讓「開始」成為視覺上最明顯的主要操作。

Settings 必須保持次要層級。

---

## 練習規則

V0.1 支援以下模式：

1. 選助詞
2. 助詞用途
3. 找錯
4. 混合

### 選助詞模式

至少需要選擇 2 個助詞。

### 助詞用途模式

可以選擇 1 個或多個助詞。

### 找錯模式

題目中必須同時包含：

- 正確句子
- 錯誤句子

### 混合模式

如果選擇 2 個以上助詞：

使用全部三種題型：

- 選助詞
- 助詞用途
- 找錯

如果只選擇 1 個助詞：

只使用：

- 助詞用途
- 找錯

---

## 課程篩選規則

課程篩選必須嚴格執行。

如果選擇課程：

`[18]`

只能使用：

`lesson === 18`

的資料。

不要自動包含第 1～17 課。

如果選擇：

`[16, 17, 18]`

只能使用：

- 第 16 課
- 第 17 課
- 第 18 課

的資料。

---

## 答題回饋

回答之後，只顯示簡短的助詞用途提示，例如：

- 時間點
- 到達點
- 動作場所
- 存在地點
- 對象
- 手段

V0.1 不要顯示長篇文法解說。

---

## 日文語音

使用者提交答案之前，不要提供完整句子的語音播放。

回答之後：

- 顯示語音播放按鈕
- 播放修正後的完整日文句子
- 即使使用者答錯，也永遠播放正確句子
- 語言使用 `ja-JP`
- 允許重複播放

---

## AI 匯出

每次練習結束時，提供一個一鍵複製功能。

可以把整次練習結果完整複製到剪貼簿。

匯出的文字必須包含：

- 本次練習設定
- 總分
- 每一題題目
- 使用者答案
- 正確答案
- 助詞用途標籤
- 課程
- 題型
- 答對 / 答錯結果
- 簡單統計資料
- 一段可以直接請 AI 分析弱點的 Prompt

輸出內容必須可以直接貼到：

- ChatGPT
- Claude
- Gemini

並直接進行分析。

---

## 內容規則

不要複製：

- 教科書練習題
- 教科書文章
- 官方音檔

所有例句都必須自行原創。

例句可以參考該課所教的助詞用法，但必須保持：

- 短
- 自然
- 初學者友善
- 語意清楚
- 不容易產生多重解讀

---

## 架構原則

題目內容必須與 UI 邏輯分離。

優先設計一個可重複使用的資料模型，讓同一份題目資料可以產生不同的練習模式。

不要為每一種題型建立一整套重複的題庫。

保持 Component 小而單純。

以下邏輯應放在 composables 或 services：

- 題目生成
- 題型轉換
- 答案判定
- 計分

不要把這些邏輯全部寫在 Page Component 裡。

---

## 品質要求

在宣告一個 Phase 完成之前，必須：

1. 執行 TypeScript type checking
2. 執行 lint
3. 執行相關測試
4. 驗證 production build
5. 檢查主要手機尺寸
6. 整理修改過的檔案與尚未解決的問題

不要隱藏：

- 測試失敗
- TypeScript 錯誤
- lint 錯誤
- build 錯誤

---

## 範圍控制

不要加入 `PRODUCT_SPEC.md` 之外的功能。

尤其不要加入：

- Authentication
- Backend API
- Database Server
- Leaderboard
- Hearts
- XP
- Social features
- Subscription
- Cloud Sync

除非之後使用者明確要求。

---

## 開發策略

採用漸進式開發。

初期 UI 開發階段，不要試圖一次完成全部 25 課。

先使用少量原創測試資料。

首先確認以下流程：

- 練習流程是否順暢
- 題型是否清楚
- 互動是否好用
- 答題邏輯是否正確
- 整體體驗是否有趣

等到整個練習互動模型穩定之後，再擴充完整題庫。