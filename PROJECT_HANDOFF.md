# PROJECT_HANDOFF.md

# Issho 專案交接文件

> 文件目的：將 Issho 的產品討論、已確認決策、目前實作狀態、限制、歷史脈絡與下一階段工作完整交接給 Codex 工作區中的下一位開發協作者。
>
> **重要原則：** 本文件不是重新設計產品的提案。凡是「已確認需求」「目前實作事實」「曾討論但未確認的提案」「待確認事項」會明確分開。接手者不得把提案自行升級成需求，也不得重做已完成的 Phase 1。

---

## 0. 專案速覽

- **專案名稱：** Issho
- **目前產品：** Mobile-first 日文助詞練習 App
- **主要技術：** Vue 3、TypeScript、Quasar CLI with Vite、Pinia
- **最終已確認方向：** PWA
- **目前進度：** Phase 1 App Shell 已完成
- **下一預定階段：** Phase 2
- **目前禁止：** 未經使用者授權，不得提前實作 Phase 2 以外的後續 Phase，也不得自行加入後端、帳號、XP、愛心、排行榜等未授權功能。

### 狀態標記

本文件使用以下標記：

- **【使用者已確認】**：使用者在討論中明確指定、修正或接受，或在本次交接訊息中明確給定。
- **【目前已實作】**：使用者提供的實際工作區狀態。
- **【現有規格】**：已被放進 `AGENTS.md` / `docs/PRODUCT_SPEC.md` 或此前正式整理的 V0.1 規格；接手者仍應以工作區實際檔案為準核對。
- **【討論中的提案】**：曾提出，但使用者沒有明確確認為最終要求。
- **【待確認】**：目前沒有足夠資訊，不應自行決定。

---

# 1. 產品願景與使用情境

## 1.1 為什麼要做 Issho

【使用者已確認】

使用者正在學《大家的日本語》，希望有一個能在日常空檔中快速反覆練習的工具。最早討論的方向包含：

- 文法
- 助詞
- 單字
- 聽力

但在產品收斂過程中，使用者明確決定：

> **第一階段先專注把「助詞」做好。**

原因不是想做一個完整電子教材，而是希望解決「課上過了，但助詞沒有熟到可以快速直覺反應」的問題。

使用者過去也曾使用簡單 HTML 日文測驗，做完後把答題紀錄貼給 AI 分析。這個使用習慣直接影響 Issho 的核心設計：App 本身負責快速練習與蒐集作答狀況，完成後可快速把結果交給 AI 做更深入的學習分析。

---

## 1.2 主要使用者

【使用者已確認】

目前的第一使用者就是專案發起人本人。

重要使用習慣：

- 主要在手機使用。
- 使用時間通常很碎片化，例如搭車、排隊、工作空檔等。
- 希望「有一點空閒就能直接打開玩」。
- 不希望每次開始前先做一堆設定。
- 喜歡完成練習後，把自己的真實作答紀錄交給 ChatGPT 等 AI 分析弱點。

因此本產品現階段不是以「多人商業 SaaS」為中心設計，而是以：

> **低摩擦、高頻率、手機碎片時間、個人學習**

作為核心。

---

## 1.3 核心痛點

【使用者已確認】

### 痛點 A：傳統測驗啟動成本太高

如果每次打開都要：

1. 選課程
2. 選助詞
3. 選模式
4. 選題數
5. 再按開始

使用者會覺得麻煩，降低實際使用頻率。

因此最後確認的首頁原則是：

> **打開就能直接按「開始」。**

設定是次要操作，不能阻擋日常開始練習。

### 痛點 B：只知道答案，不代表理解助詞功能

例如指定練 `に` 時，如果所有題目的正解都是 `に`，使用者可以無腦一直按 `に`，沒有學習價值。

因此需要不同題型去測：

- 這裡應該放哪個助詞？
- 這個助詞在句子中的用途是什麼？
- 這句話的助詞有沒有錯？

### 痛點 C：做完題後不容易把弱點交給 AI

使用者非常重視 AI 輔助學習，希望完成一組題目後，可以一鍵取得這次所有作答狀況，再直接貼給 ChatGPT / Claude / Gemini 分析。

---

## 1.4 成功標準

【使用者已確認 / 現有規格】

成功不是「功能數量很多」，而是：

> **使用者在沒有被提醒的情況下，有幾分鐘空檔時會自己想打開 Issho 做一組題。**

因此以下情況都應視為產品方向出錯：

- 首頁需要思考才能開始。
- 每次都要重新選範圍與助詞。
- 設定比做題更醒目。
- 一題要操作很多次才能進下一題。
- 回饋文字過長。
- UI 像後台系統或傳統考卷。
- 完成後很難把資料交給 AI。

---

# 2. 已確認需求與不可違反的規則

## 2.1 首頁 UX

【使用者已確認】【目前已實作】

首頁的優先順序經過多次討論後已收斂。

最終原則：

1. **首頁一打開就可以直接開始。**
2. 中央顯示目前使用中的練習設定。
3. 最醒目的操作是大型「開始」按鈕。
4. 調整練習內容使用次要的 Tune / 練習設定入口。
5. App Settings 使用 `⚙` icon，固定放在**右上角角落**。
6. 不使用底部四個 Tab。
7. 不在首頁鋪四張模式卡片。
8. 不把課程、助詞、模式做成三個常駐下拉選單。
9. 設定應記住，上次設定下次打開可直接使用。

### 已否決的首頁形式

以下形式使用者明確覺得太麻煩或太複雜：

- `🏠 學習 / 🎯 練習 / 🔁 複習 / 📊 進度` 四分頁。
- 首頁先顯示四個大模式卡片再選。
- 首頁常駐：
  - `課程 [第18課▼]`
  - `助詞 [に][で]`
  - `模式 [選助詞▼]`
- 每次開啟先進入設定流程。

請勿把上述設計重新帶回。

---

## 2.2 視覺方向

【使用者已確認】

整體希望：

> **像 Duolingo 那樣簡單、清楚、圖像化、像在玩，而不是像填表單或考卷。**

具體偏好：

- Mobile-first。
- 大型可點擊區域。
- 圓角。
- 適度 icon 化。
- 一個畫面聚焦一個主要動作。
- 正確／錯誤回饋要明顯。
- 文字越短越好。
- 正式 UI 不應依賴大量 Emoji 當主要 icon；之前規格偏向使用 Quasar / Material Icons。
- 設定可以有，但應退居次要位置。

注意：

「像 Duolingo」是指**操作哲學與簡潔遊戲感**，不是要求複製 Duolingo 的品牌、角色、插圖、商標或精確版面。

---

## 2.3 練習內容設定

【使用者已確認】【目前已實作】

練習設定目前包含三個 expandable sections：

1. 範圍
2. 助詞
3. 模式

使用者不希望看到：

- `單課 / 範圍 / 自訂`
- `に・で / 全部 / 自訂`

這種多層 radio 選擇。

最後確認為：

> **點開「範圍」後直接勾要的課；點開「助詞」後直接勾要的助詞。**

目前 Phase 1 已依此完成 UI 與 state。

---

## 2.4 課程選取與嚴格篩選規則

【使用者已確認】【不可違反】

目前第 1～25 課可以任意多選，而且允許不連續。

例如：

- `[18]`
- `[16, 17, 18]`
- `[3, 5, 18]`

### 最重要規則：單課不是「累積到這一課」

使用者特別修正過這件事。

如果只選：

```text
第 18 課
```

那麼只能出：

```text
lesson === 18
```

的資料。

**不能因為學到第 18 課，就自動混入第 1～17 課。**

只有使用者真的選：

```text
第 1～18 課
```

或勾選 1～18，才可以出這些課的內容。

同理：

```ts
selectedLessons = [16, 18]
```

只能出第 16、18 課，不能出第 17 課。

---

## 2.5 助詞選取

【使用者已確認】【目前已實作】

目前 UI 支援：

- は
- が
- を
- に
- で
- へ
- と
- も
- の
- から
- まで

使用者可直接多選／取消。

重要需求：

> 有時候使用者只想專門練某一個助詞，例如 `に`。

因此系統必須支援單一助詞的練習情境；但不同模式如何解讀單一助詞，見下一節。

---

# 3. 練習模式

目前有四個模式的 UI 與 state。

## 3.1 模式 A：選助詞

【使用者已確認】【目前 Phase 1 已完成設定 UI，真正題目尚未實作】

例：

```text
学校（　）行きます。

に
で
```

核心問題：

> **這句應該放哪個助詞？**

### 不可違反規則

使用者指出一個重要漏洞：

如果使用者指定只練 `に`，而這種模式所有正解都必然是 `に`，那麼一直按 `に` 就會通過。

因此：

> **「選助詞」模式至少必須有 2 個選取的助詞。**

Phase 1 已實作：

- selectedParticles 不足 2 個時顯示提示。
- Start 停用。

---

## 3.2 模式 B：助詞用途

【使用者已確認核心概念；部分細節待核對現有規格】

這個模式源自使用者對「只練 `に`」問題的修正。

不是問：

```text
7時（　）起きます。
に / で / を / へ
```

而是直接顯示：

```text
7時に起きます。
```

再問：

```text
「に」在這裡表示什麼？

時間點
到達點
動作場所
對象
```

核心目標：

> **理解同一助詞在不同句子裡扮演的功能。**

曾討論的用途例：

```text
に
├─ 時間點
├─ 到達點
├─ 存在地點
├─ 對象
└─ ...
```

### 待注意

此前正式規格曾寫成「用途模式可使用一個或多個 selectedParticles」。

但使用者最明確的原始情境是：

> 「我有時候只想練某個助詞，像 `に`。」

因此：

- **支援單一助詞：已確認。**
- **多選助詞時用途模式的精確行為：請先核對目前 `PRODUCT_SPEC.md` 與程式碼；若仍有歧義，不要自行重定義。**

---

## 3.3 模式 C：找錯

【使用者已確認】

使用者明確要求加入「找錯模式」。

例如：

```text
図書館に本を読みます。

✓ 沒問題
👀 有問題
```

如果錯誤：

```text
に → で
動作場所
```

### 重要規則

不能所有題目都是錯的。

必須混入：

- 正確句
- 錯誤句

否則使用者可以每題一直選「有問題」。

例：

```text
学校に行きます。
```

本身可能就是正確句。

---

## 3.4 模式 D：混合

【使用者已確認存在這個模式】【目前 Phase 1 已完成 UI 與 state】

使用者希望可以選「混合模式」，避免每次都固定一種題型。

此前規格討論中的行為：

- 選取 2 個以上助詞時：可混合「選助詞 / 助詞用途 / 找錯」。
- 只選 1 個助詞時：因「選助詞」模式失去意義，可只混合「助詞用途 / 找錯」。

以上行為曾被寫入先前規格，但**使用者沒有逐條重新確認混合比例、隨機演算法與題型比例**。

因此：

- 混合模式存在：已確認。
- 精確混合比例：**待確認 / 先遵守現有 `PRODUCT_SPEC.md`，不要自行創造新策略。**

---

# 4. 答題回饋

## 4.1 回饋要非常短

【使用者已確認】

使用者不希望每題答完出現很長的文法解說。

例如答完只需要：

```text
✅ に
時間點
```

或：

```text
❌

正解：で
動作場所
```

已討論的短用途標籤包括：

- 時間點
- 到達點
- 動作場所
- 存在地點
- 對象
- 動作對象
- 手段
- 起點
- 終點

實際 taxonomy 仍應由題庫內容與 `PRODUCT_SPEC.md` 定義，不要為了擴充而自行加一大套語言學分類。

---

## 4.2 每題答完後顯示整句語音按鈕

【使用者已確認】【尚未實作】

這是使用者後來明確新增的功能。

規則：

1. **答題前不要顯示會暴露答案的完整句語音。**
2. 答案提交後，出現語音 icon。
3. 點擊後播放**正確完整句子**。
4. 如果原本答錯，仍播放修正後的正確句。

例：

題目：

```text
図書館（　）日本語を勉強します。
```

答案：

```text
で
```

答完後播放：

```text
図書館で日本語を勉強します。
```

### 技術方向

此前規格規劃 V0.1 使用 Browser / Web Speech API，語言 `ja-JP`。

這是規格／技術方案，尚未在 Phase 1 實作。

---

# 5. App Settings

【使用者已確認】【目前已實作偏好保存】

右上角 `⚙` 為 App 本身設定。

它應固定在右上角，不與「開始」搶視覺焦點。

目前只有：

- 音效 ON/OFF
- 日文語音 ON/OFF

### 明確否決

使用者明確要求：

> **不要有震動設定。**

因此請勿重新加入：

```text
📳 震動 ON/OFF
```

目前 Phase 1：

- `soundEnabled` 已保存。
- `japaneseSpeechEnabled` 已保存。
- **尚未真正播放音效或 TTS。**

---

# 6. 完成畫面與 AI Copy

## 6.1 完成畫面

【已確認方向】【Phase 2 尚未實作】

不要做成 dashboard。

主要只需要：

```text
完成！

8 / 10

[ 再來一組 ]

[ 複製給 AI ]
```

重點仍是快速繼續，或把資料交給 AI。

---

## 6.2 一鍵複製給 AI

【使用者已確認】【尚未實作】

這是 Issho 的核心功能之一，不只是附加功能。

使用者明確原因：

> 自己很喜歡把學習狀況給 AI 分析，一鍵複製後可以直接貼到 ChatGPT，更快學習。

完成整組練習後，需要能一鍵複製本次作答狀況。

### 應包含的資訊

此前正式規格整理為：

- 練習設定
- 課程
- 選取助詞
- 模式
- 題數
- 答對／答錯
- 每一道題
- 使用者答案
- 正確答案
- 用途標籤
- lesson
- exercise type
- correct / incorrect
- 簡單統計
- 一段可以直接請 AI 分析的提示文字

範例目的：

```text
請分析我這次的日文助詞練習。

1. 找出我真正不熟的助詞用法
2. 找出容易混淆的地方
3. 用簡單白話重新教我
4. 再針對弱點出幾題
```

### 準確性注意

「一鍵複製完整作答狀況」是使用者已確認。

輸出文字的**精確 schema / prompt wording** 是此前規格整理出來的方案，不需要在 Phase 2 提前實作，未來實作 AI Copy 時應以 `PRODUCT_SPEC.md` 為準並允許使用者再調整。

---

# 7. 《大家的日本語》內容與資料規則

## 7.1 課程對應方式

【使用者已確認】

App 以《大家的日本語》的課程進度作為練習骨架。

但「第 18 課」的定義不是：

> 使用者已經學會第 1～18 課，所以第 1～17 課都可混進來。

而是：

> 題目／用法資料本身被明確歸屬到哪一課。

因此 `lesson` 是一個重要內容標記。

---

## 7.2 原創例句

【現有規格；使用者未反對】【不可把教材整本搬進 App】

此前正式產品規格已定：

- 不大量複製課本練習題。
- 不大量複製課文。
- 不使用官方音檔當作自建產品內容。
- 應依《大家的日本語》的學習範圍設計**原創例句**。

原創例句原則：

- 短。
- 自然。
- 初級程度可理解。
- 優先使用該課或之前已學過的單字與文法。
- 一題聚焦一個主要判斷點。
- 避免答案模糊。
- 若兩種助詞在語境上都可能成立，要避免當作單一正解題，除非題目本身就是在教差異。

---

## 7.3 `lesson` 的資料語意

【使用者已確認核心行為】【現有規格】

曾討論的定義：

```ts
lesson: number
```

代表：

> 這筆助詞用法／例句在 Issho 題庫裡被歸屬到《大家的日本語》的哪一課。

它不是單純：

> 句子中某個單字第一次出現的課數。

篩題時必須依這個欄位嚴格過濾。

---

## 7.4 `usage` 用途標籤

【使用者已確認需要簡短用途】【資料模型細節尚未實作】

每一題需要知道不只：

```text
answer = に
```

還要知道：

```text
usage = 時間點
```

或：

```text
usage = 到達點
```

原因：

1. 每題答完要提供超短解答。
2. 「助詞用途」模式需要這個答案。
3. 未來 AI Copy 才能分析：
   - `に` 整體錯很多？
   - 還是只有 `に = 對象` 不熟？
4. 同一助詞不同功能可以被分開觀察。

---

## 7.5 曾討論的資料模型

【討論中的提案 / 尚未正式實作題庫】

此前曾提出一個統一資料模型概念，讓一筆例句可以支援多種題型，而不是每種模式各建一套重複題庫。

概念範例：

```ts
interface ParticleExample {
  id: string
  lesson: number

  correctSentence: string
  sentenceWithBlank: string

  particle: string
  usage: string

  distractorParticles: string[]
  usageChoices: string[]

  wrongVariant?: {
    sentence: string
    wrongParticle: string
  }

  shortExplanation: string
  audioText: string
}
```

這個模型曾被放入先前的產品企劃，但**Phase 1 尚未實作真正題庫，因此不能假設目前程式碼已採用此介面**。

接手者應：

1. 先讀 `docs/PRODUCT_SPEC.md`。
2. 看目前 repository 的 types。
3. Phase 2 真正設計題庫時，保留已確認的不變條件：
   - lesson
   - correct sentence
   - particle
   - usage
   - 可生成選助詞題
   - 正確完整 audio sentence
4. 不要因為這份歷史 interface 範例而強迫重構現有 Phase 1。

---

# 8. 重要產品決策與理由

## 8.1 先做 PWA，而不是先做原生 App

【已確認最終目標包含 PWA】【原生 App 為早期提案，非目前要求】

早期曾討論：

- 原生 App
- Web
- PWA
- Quasar + Capacitor

收斂方向：

> 先做 mobile-first App UX，技術上以 PWA 為最終目標。

理由：

- 主要使用情境就是手機。
- PWA 可以維持 Web 技術棧。
- 不需要第一版就承擔 App Store / native 開發成本。
- Quasar 適合 Vue 團隊做 mobile-first + PWA。

### 不要誤讀

「未來可用 Capacitor 包 iOS / Android」曾被提出，但本次交接中使用者只明確說：

> 最終目標包含 PWA。

因此：

- PWA：已確認。
- Capacitor / 原生包裝：**未授權的未來可能性，不是現階段 roadmap 承諾。**

---

## 8.2 為什麼不是 Nuxt

【早期技術取捨背景，不需重新討論】

早期評估認為此產品本質是手機互動 App，而不是 SEO / content-first 網站，因此選擇 Quasar + Vue，而非以 Nuxt 為主。

目前專案已經是：

- Vue 3
- TypeScript
- Quasar CLI with Vite
- Pinia

Phase 1 已完成。

**不要重新初始化技術棧，也不要把專案改成 Nuxt。**

---

## 8.3 為什麼第一版只做助詞

【使用者已確認】

最初產品想像包含：

- 文法
- 助詞
- 單字
- 聽力

但使用者主動收斂：

> 先專注把「助詞」做好。

原因：

- 先把一個核心 loop 做到真的願意反覆使用。
- 不想因功能太多而變成複雜學習平台。
- 助詞特別適合短題、快速判斷與反覆練習。

因此 V0.1 不應因「完整日文 App」的願景而提前加入其他學習模組。

---

## 8.4 為什麼不要四個首頁 Tab

【使用者明確否決】

曾提出：

```text
🏠 學習
🎯 練習
🔁 複習
📊 進度
```

使用者認為：

> 多鄰國也沒有必要讓開始練習這麼麻煩；要簡單才會一直玩。

因此首頁後續才一路縮減成：

> **打開 → 開始**

這個背景很重要：任何後續功能都不能再把首頁變回入口型 Dashboard。

---

## 8.5 為什麼不在首頁直接選課程／助詞／模式

【使用者明確否決】

曾經設計：

```text
課程 [ 第18課 ▼ ]
助詞 [ に ][ で ] ＋
模式 [ 選助詞 ▼ ]

開始
```

使用者仍覺得太麻煩。

最終做法：

- 首頁只顯示目前設定。
- 調整練習內容走 Tune。
- App Settings 走右上角齒輪。
- 設定會保存。

理由：

> 使用者平常不想「配置 App」，只想開始玩。

---

## 8.6 為什麼課程與助詞採直接多選

【使用者明確修正】

曾提出：

```text
範圍
● 第18課
○ 第16～18課
○ 自訂範圍

助詞
● に・で
○ 全部
○ 自訂
```

使用者認為仍有點麻煩。

最後改為：

- 點「範圍」→ 直接勾第 1～25 課。
- 點「助詞」→ 直接勾助詞。

不需要先選「單課 / 範圍 / 自訂」。

---

## 8.7 為什麼「只練 に」不能用普通選助詞題

【使用者主動指出問題】

最早曾提議：

「指定 `に` 後，題目仍讓使用者判斷該不該用 `に`。」

使用者指出：

> 這樣還是可以一直按 `に` 通過。

因此改成：

> 指定某助詞時，可透過「助詞用途」測它在句子中的功能。

這個修正是重要學習設計決策。

---

## 8.8 為什麼要 AI Copy，而不是第一版內建 AI

【使用者使用習慣 + 既有產品方向】

使用者本來就會：

1. 做題。
2. 把結果貼給 ChatGPT。
3. 要 AI 找弱點、重新解釋、再出題。

因此 Issho 第一版不需要先建完整 AI backend。

更符合現階段的方式是：

> **Issho 負責練習與紀錄 → 一鍵 Copy → 外部 AI 深度分析。**

這能讓 V0.1 保持簡單，也符合使用者真實習慣。

---

# 9. 使用者偏好與合作方式

## 9.1 UI / UX 偏好

【使用者已確認】

使用者偏好：

- 簡單。
- 快。
- 圖像化。
- 手機優先。
- 有「玩」的感覺。
- 一打開就知道主要按哪裡。
- 設定藏起來。
- 不要像企業後台。
- 不要像長考卷。
- 不要很多文字。
- 答完只要簡單說明，例如「到達點 / 動作場所 / 時間點」。

使用者會直接指出「太麻煩」「太多」「這樣可以作弊」等問題。

接手協作者應把這類回饋視為高優先級 UX 約束，而不是單純視覺偏好。

---

## 9.2 開發節奏

【使用者已確認 / 現有規格】

採分 Phase 開發。

原則：

1. 一個 Phase 做完。
2. 驗證。
3. 使用者確認。
4. 再進下一階段。

不要一次叫 Agent 把整個 App 全部做完。

尤其：

- 不要 UI 還沒確認就大量製作第 1～25 課題庫。
- 不要提前實作尚未授權 Phase。
- 不要看到 roadmap 就自動把全部 roadmap 完成。

---

## 9.3 驗收方式

【目前 Phase 1 實作事實】

Phase 1 已使用：

- lint
- TypeScript check
- production build
- 單元測試
- 瀏覽器測試
- 多 viewport 檢查

因此後續應延續：

- 先完成本 Phase。
- 跑檢查。
- 報告修改內容與問題。
- 不隱藏錯誤。
- 不用「看起來能跑」取代實際驗證。

---

# 10. V0.1 範圍與禁止事項

## 10.1 V0.1 已確認核心

目前 V0.1 圍繞助詞練習，包含已規劃／已確認的：

- Mobile-first UI
- 課程範圍多選
- 助詞多選
- 選助詞
- 助詞用途
- 找錯
- 混合
- 10 題 session
- 進度
- 答案判定
- 簡短用途回饋
- 整句日文語音
- 音效設定
- 日文語音設定
- 一鍵複製給 AI
- PWA / offline（後續 Phase）

---

## 10.2 現階段不能自行加入

【現有 AGENTS / 產品限制】

除非使用者未來明確要求，不要自行加入：

- Authentication
- Backend API
- Database Server
- 帳號系統
- Leaderboard
- Hearts
- XP
- Social features
- Subscription
- Cloud sync

也不要自行把：

- 單字
- 文法
- 正式聽力課程
- 初級 II
- AI 內建分析

提前變成 V0.1 開發工作。

---

# 11. 更遠期構想：只作背景，不是目前需求

## 11.1 文法、單字、聽力

【早期產品願景 / 尚未授權實作】

使用者最初確實想像過完整的《大家的日本語》練習 App，可以做：

- 文法
- 助詞
- 單字
- 聽力

但目前已明確先聚焦助詞。

因此這些是**產品長期可能方向**，不是接下來工作的 backlog。

---

## 11.2 間隔重複、弱點自動加權

【討論中的提案，未確認為 V0.1】

曾提出：

- 記錄容易錯的 `に vs で`
- 未來提高弱點題目的出現機率
- 間隔重複
- FSRS 類型排程

使用者沒有在本輪把這些確定為 V0.1。

不能自行加入 Phase 2。

---

## 11.3 Python Content Pipeline

【討論中的提案，不是目前技術要求】

因歷史上曾使用 Python 產生 HTML 測驗，曾討論未來 Python 可用來：

- 產生題庫
- 驗證 JSON / YAML
- 檢查重複題
- Content Pipeline

但 Issho 目前正式技術棧並不包含「必須使用 Python」。

因此不要因歷史原型而自行新增 Python build pipeline。

---

## 11.4 Capacitor / Native App

【早期提案 / 尚未承諾】

曾討論 Quasar 未來可透過 Capacitor 包成 iOS / Android。

本次交接明確確認的最終目標只有：

> **PWA**

Native packaging 請視為未來可能選項，不是目前 roadmap 承諾。

---

# 12. 目前實際開發狀態

以下由使用者在本次交接要求中明確提供，視為目前工作區事實。

## 12.1 工作區已有文件

【目前已實作】

- `AGENTS.md`
  - 開發規則與產品限制。
- `docs/PRODUCT_SPEC.md`
  - 主要產品規格。
- `README.md`
  - 目前實作範圍、結構與執行方式。

### 接手注意

本交接文件不能取代上述三個檔案。

Codex 接手後必須一起閱讀，並以目前 repository 實際程式碼驗證是否一致。

---

## 12.2 Phase 1 已完成

【目前已實作】

1. Mobile-first Quasar Layout。
2. 簡潔首頁：
   - 右上 App Settings。
   - 中央目前練習設定。
   - 大型「開始」。
   - 下方 Tune。
3. 練習設定底部 panel：
   - 範圍
   - 助詞
   - 模式
   - expandable sections
4. 第 1～25 課圓形按鈕。
   - 支援任意多選。
   - 支援不連續多選。
5. 助詞多選：
   - は
   - が
   - を
   - に
   - で
   - へ
   - と
   - も
   - の
   - から
   - まで
6. 四種模式的 UI 與 state：
   - 選助詞
   - 助詞用途
   - 找錯
   - 混合
7. App Settings：
   - 音效 ON/OFF
   - 日文語音 ON/OFF
   - 目前只保存偏好
8. localStorage 保存五項設定，支援重新整理與重新開啟。
9. 空選擇與「選助詞模式不足兩個助詞」：
   - 顯示提示。
   - 停用開始。
10. 損壞的儲存資料與儲存失敗處理。

---

## 12.3 目前預設值

【目前已實作】

```ts
selectedLessons = [18]
selectedParticles = ['に', 'で']
practiceMode = '選助詞'
soundEnabled = true
japaneseSpeechEnabled = true
```

實際 enum / internal key 名稱請以程式碼為準，不要因本文件中文標籤自行改 state shape。

---

## 12.4 目前架構

【目前已實作】

已拆分為：

- `MainLayout`
- 首頁與路由容器
- `SettingsPanel`
- `PracticeSettings`
- `AppSettings`
- Pinia settings store
- settings types
- settings service
- 單元測試
- 瀏覽器測試

### 注意

本交接文件沒有讀到目前 Codex 工作區的實際原始碼，因此：

- 不提供虛構檔名。
- 不假設各 component 的確切路徑。
- 不要求重構上述已存在架構。

接手者應先讀 repository。

---

## 12.5 Phase 1 驗證紀錄

【目前已實作】

Phase 1 完成時：

- lint：通過。
- TypeScript check：通過。
- production build：通過。
- 5 項單元測試：通過。
- 6 項瀏覽器測試：通過。

已檢查 viewport：

- 375 × 667
- 390 × 844
- 393 × 852
- 430 × 932
- Desktop

### 尚未完成

- **手機實機驗證尚未進行。**

### 已知非阻擋問題

- 工具鏈有 `rootDir` 提示。
- 目前為非阻擋項目。

不要為了「順手整理」在 Phase 2 開始前自行做大規模工具鏈重構；若需要處理，先說明原因。

---

## 12.6 目前 Start 行為

【目前已實作】

目前「開始」按鈕尚未進入真正題目流程。

它只會顯示：

> 練習尚未開放

或同等意義的提示。

這是正常 Phase 1 狀態，不是 bug，也不是需要重建 App Shell。

---

# 13. 尚未實作

【目前狀態】

目前以下都**尚未完成**：

- 真正題庫
- 作答 Engine
- 10 題 session
- 計分
- 題目進度
- 答案判定
- 真正的簡短用途回饋流程
- 下一題 flow
- 完成畫面
- TTS
- 整句語音播放
- 真正的答對／答錯音效
- AI Copy
- PWA
- Offline
- Backend
- 帳號功能

請勿因這些曾經在規格中出現，就誤寫成 Phase 1 已完成。

---

# 14. 下一預定階段：Phase 2

【使用者已確認】

下一個預定階段為：

> **使用第 18 課少量原創例句，建立「選助詞」的 10 題 session。**

Phase 2 應包含：

1. 第 18 課少量原創例句 Sample Data。
2. 選助詞模式。
3. 10 題 Session。
4. 進度條。
5. 答案判定。
6. 簡短用途回饋。
7. 下一題。
8. 完成畫面。

### Phase 2 邊界

【不可違反】

目前尚未授權提前實作其他 Phase。

因此 Phase 2 不應順便完成：

- 助詞用途完整 Engine
- 找錯完整 Engine
- 混合 Engine
- TTS
- 音效播放
- AI Copy
- PWA/offline
- 後端
- 帳號
- 1～25 課完整題庫

除非使用者下一則指示改變範圍。

---

# 15. 先前共識中的後續 Phase

以下是此前整理過的開發順序，**只能當 roadmap，不代表現在已授權執行全部**。

## Phase 1 — App Shell

【已完成】

- Layout
- 首頁
- Settings
- Practice Settings
- localStorage
- responsive

---

## Phase 2 — 核心答題

【下一階段】

- 第 18 課少量原創 Sample Data
- 10 題 Session
- 選助詞
- 進度
- 判定
- 短用途
- 下一題
- 完成畫面

---

## Phase 3 — 其他題型

【未實作 / 尚未授權開始】

加入：

- 助詞用途
- 找錯
- 混合

並建立可重用的 Exercise Engine。

---

## Phase 4 — 聲音

【未實作 / 尚未授權開始】

加入：

- 答對音效
- 答錯音效
- 完成音效
- soundEnabled 真正生效
- 日文 TTS
- 答題後整句播放
- japaneseSpeechEnabled 真正生效

---

## Phase 5 — AI Copy

【未實作 / 尚未授權開始】

加入：

- 本次 Session 作答紀錄
- 簡單統計
- Clipboard
- AI-friendly export
- 已複製回饋

---

## Phase 6 — PWA

【未實作 / 尚未授權開始】

加入：

- Manifest
- App Icon
- Service Worker
- PWA build
- Installability
- Offline 行為
- 手機實機驗證

---

## Phase 7 — 正式題庫

【未實作 / 尚未授權開始】

在互動模式確認好用後，才擴充：

- 第 1～25 課正式題庫

不要在 Phase 2 就一次生成大量題目。

---

# 16. 待決事項與可能矛盾

接手者遇到以下項目不得自行假定。

## 16.1 專案名稱與歷史文件名稱

【待核對】

本次使用者明確指定專案名稱：

> **Issho**

但早期我們整理過的 `AGENTS.md` 草稿標題曾使用過類似：

> `Project: Minna Particle Practice`

不知道目前實際工作區是否已改成 Issho。

### 接手方式

- 先讀目前 `AGENTS.md` / README / package metadata。
- 若名字不一致，**列為差異**。
- 不要自動全專案 rename，等使用者指定。

---

## 16.2 助詞用途模式可否多選助詞

【待確認】

已確認：

- 單一 `に` 必須可以練用途。

此前規格曾接受／記錄：

- 用途模式可以一個或多個助詞。

但此點沒有被使用者單獨再確認。

若 Phase 3 實作時仍有模糊：

- 先依 `PRODUCT_SPEC.md`。
- 若程式／規格互相矛盾，再詢問使用者。

---

## 16.3 混合模式的題型比例

【待確認】

已確認存在「混合」。

但沒有確認：

- 每個題型固定比例？
- 完全 random？
- 如何避免連續同型？
- 單一助詞時的比例？

此前規格只定義可用題型的邊界。

不要自行做複雜 adaptive algorithm。

---

## 16.4 使用者是否能調整每組題數

【討論中的提案 / 目前不需要】

此前 V0.1 以 10 題為預設。

Phase 2 本次已明確指定 10 題 Session。

曾提過未來可能 5 / 10 / 20，但沒有被確認為需求。

因此現在不要加題數設定。

---

## 16.5 是否自動進下一題

【曾討論的提案，未確認】

曾提出：

- 答對 0.5 秒後自動下一題。
- 答錯停留看解析。

使用者沒有確認這個機制。

目前正式概念仍有「下一題 / 繼續」。

Phase 2 不要自行加入自動跳題，除非 `PRODUCT_SPEC.md` 明確如此或使用者再確認。

---

## 16.6 TTS 是否可無限重播

【曾討論的提案，未明確確認】

使用者要求：

> 每題答完出現語音按鈕，按下去可以播放整句。

曾建議可重播不限次數，但使用者沒有特別確認。

Phase 4 實作時應以產品規格為準；若規格沒寫，先保持最簡單可播放行為，不需自行增加限制或遊戲規則。

---

## 16.7 AI Export 的精確文字格式

【核心已確認，格式細節待實作時核對】

確認：

- 一鍵複製本次全部作答狀況。
- 目的為貼給 AI 分析。

未逐字確認：

- Prompt 文案。
- 欄位順序。
- 統計格式。
- 是否固定要求 AI 再出 5 題。

Phase 5 實作時應先看 `PRODUCT_SPEC.md`，必要時給使用者預覽 export 範例再定稿。

---

## 16.8 `lesson` 內容編輯準則的細粒度

【部分待確認】

已確認嚴格 lesson filtering。

但「某個助詞用法到底歸第幾課」需要依實際《大家的日本語》進度整理。

不能單靠模型印象大量生成正式題庫。

Phase 2 只有第 18 課少量 Sample Data，內容仍需確保與第 18 課定義一致。

---

# 17. 重要資料、文件與附件索引

## 17.1 Issho 工作區必讀文件

【使用者明確表示已存在】

### `AGENTS.md`

用途：

- 開發規則。
- 產品限制。
- Scope discipline。
- 技術與品質要求。

**本聊天無法直接看到目前 Codex 工作區檔案內容。**

接手者必須讀 repository 現檔，不可只靠本交接。

---

### `docs/PRODUCT_SPEC.md`

用途：

- 主要產品規格。
- V0.1 行為。
- 模式與 UX 規則。
- 開發 Phase。

**本聊天無法直接驗證目前工作區版本是否與早期草稿完全相同。**

---

### `README.md`

用途：

- 目前實作範圍。
- 專案結構。
- 執行方式。

**本聊天未取得目前 README 內容。**

---

## 17.2 目前程式碼

【需要 Codex 自己讀】

本交接沒有取得 Issho repository 的實際程式碼快照。

因此本文件不應被用來推斷：

- 真正目錄位置。
- package scripts。
- 測試框架設定。
- store 的 exact API。
- component props。
- route path。
- CSS token。

這些以 repo 為準。

---

## 17.3 歷史 N5 HTML 練習原型

以下檔案存在於使用者的 ChatGPT Library，屬於 Issho 之前的日文練習原型。

它們不是目前 Issho source code，也不是 Phase 2 必須依賴的程式碼。

### `N5_practice_with_report.html`

已知用途：

- N5 練習。
- 瀏覽器日文語音。
- 做完可產出報告貼給 ChatGPT。

可作為：

- AI Copy 使用習慣的歷史參考。
- 早期報告形式參考。

---

### `N5_minimum_practice_with_listening.html`

已知用途：

- 瀏覽器 Speech Synthesis。
- `SpeechSynthesisUtterance`
- `ja-JP`
- 日文 voice 選擇。
- 整體測驗計分。

可作為：

- Phase 4 TTS 的歷史 prototype 參考。

注意：

Issho 現在應依新架構實作，不要直接把舊 HTML JS 貼進 Vue component。

---

### `N5_targeted_review_no_hint.html`

已知用途：

- 無提示錯題複習。
- 題目先作答再看解析。
- 歷史弱點練習 prototype。

---

### `N5_weakness_rescue_15_no_hint.html`

已知用途：

- 專攻上一輪錯誤。
- 不新增範圍。
- 無提示。
- 歷史 adaptive / weakness-review 思路參考。

---

### `N5_final_patch_16_no_hint.html`

已知用途：

- 針對特定錯誤類型補題。
- 歷史弱點補強 prototype。

---

### 是否需要轉交這些 HTML？

**不是目前 Phase 2 必要附件。**

若希望 Codex 研究過去操作習慣，可選擇性轉入 `/references/legacy/` 或另外提供。

不要讓這些檔案污染正式 `src/`，也不要把其舊 UI 當成目前設計基準。

---

## 17.4 歷史討論引用的官方資料

以下是早期技術選型曾參考過的來源，屬於參考資料，不是 repository artifact：

- Quasar Quick Start / Quasar CLI with Vite
  - https://quasar.dev/start/quick-start/
- Quasar PWA
  - https://quasar.dev/quasar-cli-vite/developing-pwa/preparation/
- MDN Web Speech API / SpeechSynthesis
  - https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesis
  - https://developer.mozilla.org/en-US/docs/Web/API/SpeechSynthesisUtterance
- Capacitor Docs
  - https://capacitorjs.com/docs
  - 僅屬早期 future-native 選項，目前非已確認 roadmap。
- OpenAI Codex / AGENTS.md 工作方式
  - 早期用來決定 repository 內應保存產品與開發規則。

接手者不需要在 Phase 2 為了這些連結重新研究技術選型。

---

## 17.5 圖片 / Figma / 正式視覺稿

【目前沒有已知必要附件】

產品 UI 主要是透過文字 wireframe 與實作迭代確定。

本交接範圍內沒有：

- 正式 Figma。
- 使用者確認的完整 design system 圖檔。
- 必須轉交的產品 mockup 圖片。

因此 Phase 1 現有畫面就是目前最接近實際 UI source of truth 的成果。

---

# 18. 使用者需要另外轉交給 Codex 的東西

若 Codex 已在 Issho repository 工作區中，應至少確保以下檔案都可讀：

1. `PROJECT_HANDOFF.md`（本文件）
2. `AGENTS.md`
3. `docs/PRODUCT_SPEC.md`
4. `README.md`
5. 現有完整 repository

可選但非必要：

- 舊 N5 HTML prototype 檔案（只作歷史參考）

目前沒有其他必須的圖片或附件。

---

# 19. 接手者第一步：不要寫 Code

接手者第一件事不是 Phase 2 實作。

應先完成一次 repository 對齊。

需要閱讀：

1. `PROJECT_HANDOFF.md`
2. `AGENTS.md`
3. `docs/PRODUCT_SPEC.md`
4. `README.md`
5. 目前 source code
6. package / test / build 設定

然後輸出：

### A. 一致之處

例如：

- Phase 1 code 是否符合 handoff。
- settings 預設是否一致。
- 四個 modes 是否都有 UI state。
- validation 是否已存在。

### B. 差異

例如：

- 文件名稱。
- project naming。
- store 欄位。
- PRODUCT_SPEC 與實作不一致。
- README 落後。

### C. 必要待確認事項

只列出會阻擋下一項工作或確實矛盾的問題。

不要為了小偏好發散十幾個問題。

### D. 不要自行修改

完成對齊後：

> **等待使用者指定下一項工作。**

即使 roadmap 已寫 Phase 2，也不要自行開始。

---

# 20. 給接手 Codex 的第一則訊息

請將以下訊息直接交給新的 Codex 工作區：

```text
你現在接手 Issho 專案。

先不要修改任何程式碼，也不要開始 Phase 2。

請先完整閱讀：

1. PROJECT_HANDOFF.md
2. AGENTS.md
3. docs/PRODUCT_SPEC.md
4. README.md
5. 目前 repository 的程式碼與測試／build 設定

PROJECT_HANDOFF.md 是產品討論與交接脈絡；
AGENTS.md 是開發規則與限制；
docs/PRODUCT_SPEC.md 是主要產品規格；
README.md 與目前程式碼反映實際實作狀態。

閱讀完成後，請只做一次「接手檢查」，整理：

1. 交接文件、AGENTS.md、PRODUCT_SPEC.md、README.md 與目前程式碼彼此一致的地方。
2. 任何實際差異或矛盾。
3. 哪些差異真的需要我確認才能繼續。
4. 目前 Phase 1 是否與交接描述一致。
5. 你認為下一步工作在現有文件中的既定範圍是什麼，但不要開始實作。

不要自行補需求。
不要重新設計產品。
不要把「討論中的提案」升級成已確認需求。
不要重建已完成的 Phase 1。
不要提前實作其他 Phase。
不要因為看到 roadmap 就開始寫 code。

整理完後停下來，等我指定下一項工作。
```

---

# 21. 最後提醒

Issho 最重要的產品哲學不是功能完整度，而是：

> **打開就玩。**

目前 Phase 1 已經完成用來支撐這個理念的 App Shell。

後續每次加入功能都應問：

> 這會讓使用者更快開始、更多練習，還是反而增加操作與思考成本？

但這不是授權 Agent 自行刪改需求的原則。

如果規格與這個理念看起來衝突：

1. 先指出衝突。
2. 等使用者決定。
3. 不自行重設計。

---

## 尚未涵蓋的內容

目前沒有刻意省略已知且會影響近期產品／開發的重大決策。

以下內容因本聊天無法直接讀取 Codex 工作區，因此本文件無法驗證：

- `AGENTS.md` 現行全文。
- `docs/PRODUCT_SPEC.md` 現行全文。
- `README.md` 現行全文。
- Phase 1 實際 source code。
- 真正 package version / lockfile 狀態。
- 5 項單元測試與 6 項瀏覽器測試的具體 case 名稱。
- `rootDir` 提示的完整 log。
- 手機實機行為。

這些應由接手 Codex 在第一輪接手檢查中從 repository 取得，而不是由本文件猜測。
